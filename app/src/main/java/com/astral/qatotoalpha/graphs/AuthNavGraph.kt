package com.astral.qatotoalpha.graphs

/*
fun NavGraphBuilder.authNavGraph(navController: NavHostController) {
    navigation(
        route = Graph.AUTHENTICATION,
        startDestination = Screen.SignInScreen.route
    ) {
        composable(route = Screen.SignInScreen.route) {
            //ScreenContent(name = Screen.SignInScreen.route) {}
        }
        composable(route = Screen.SignInPassScreen.route) {
            //ScreenContent(name = Screen.SignInPassScreen.route) {}
        }
        composable(route = Screen.ForgotPassScreen.route) {
            //ScreenContent(name = Screen.ForgotPassScreen.route) {}
        }
        composable(route = Screen.NewPassScreen.route) {
            //ScreenContent(name = Screen.NewPassScreen.route) {}
        }
        composable(route = Screen.SignUpScreen.route) {
            //ScreenContent(name = Screen.SignUpScreen.route) {}
        }
    }
}*/
